# apk-ble-hunting Passive Report Consumption

Tracks which `*_passive.md` byte-level advertisement guides from the sibling
`apk-ble-hunting` project have been consumed into adwatch parsers/plugins.

- **Source dir**: `/home/pi/xfer/vibin/apk-ble-hunting/reports/`
- **Report shape**: one `<slug>_passive.md` per target, byte-level decode of
  manufacturer data, service data, and device name. See the project CLAUDE.md
  for the full stage-4b pipeline.
- **Expect more**: this list grows as the apk-ble-hunting project batches new
  targets. When kicking off work, re-`ls` the reports dir and append any new
  `*_passive.md` entries below with status `pending`.

## Status legend

- **consumed** — report read, findings merged into an adwatch parser/plugin,
  no further work expected from it
- **covered** — adwatch already has a matching plugin that predates the
  report; still needs a pass to cross-check/enrich from the report
- **pending** — report exists, not yet reviewed
- **skip** — intentionally not pursued (connection-only, encrypted, no useful
  passive shape); reason noted inline

## Workflow

1. Pick a `pending` row.
2. Read `reports/<slug>_passive.md`. If it documents a useful passive shape
   (manufacturer-data prefix, service-UUID, service-data layout, or name
   regex), add/extend a plugin under `src/adwatch/plugins/`.
3. Flip to `consumed`, note the plugin file, and add a short note on what was
   actually usable.
4. If it's connection-only or encrypted-opaque, mark `skip` with reason.

## Report inventory

| # | Slug | Category | Status | adwatch plugin | Notes |
|---|------|----------|--------|----------------|-------|
| 1 | agilexrobotics-mammotion | robotic mower | consumed | `plugins/mammotion.py` (new) | company_id 0x01A8 + name regex (Luba/Yuka/Spino/RTK/etc.); product-id 4-byte ASCII decode (HM430/MN232/PC100); MAC in payload per subtype |
| 2 | aiper-link | pool cleaner | consumed | `plugins/aiper.py` (new) | name prefix `Aiper` + protocol-flag byte extraction; minimal passive surface (connection-only state) |
| 3 | allegion-leopard | smart lock (Schlage) | consumed | `plugins/schlage.py` (new) | uWeave service UUID `1f6b43aa-…` + name regex (SENSE/schlage/NDE/Walton/GSELENT/SSELENT); Encode serial extraction from `schlage<serial>` name |
| 4 | anovaculinary-android | sous vide | consumed | `plugins/anova.py` (new) | Neuron (`0e140000-…`) + SDK (`09fa0000-…`) service UUIDs + name prefix; no passive telemetry |
| 5 | apptionlabs-meater-app | meat thermometer | consumed | `plugins/meater.py` | **major upgrade from detection-only**: 11 service UUIDs (4 probe + 7 block variants); 28-entry product-type table; block status-mode decode; 3-path parser (block-normal 8B / keep-alive 10B / probe 9B); name fallback `<hex_type>-<hex_id>`; identity hash uses stable 64-bit device_id |
| 6 | ascensia-contour-us | glucose meter | consumed | `plugins/ascensia_contour.py` (new) | company_id 0x0167 + name `^(Contour\|Portal)`; detection-only (no passive state) |
| 7 | assaabloy-yale | smart lock | consumed | `plugins/august_yale.py` | added `0x0016` company + V1/V2/V4 service UUIDs + keypad service UUIDs; 16-byte LockID extraction (2-byte-header + tail-aligned); generation V1/V2/V3/V4 from UUID; keypad-serial regex → device_kind + identity hash |
| 8 | august-luna | smart lock | consumed | `plugins/august_yale.py` | merged with Yale; identity now prefers stable LockID over mac+name |
| 9 | barkinglabs-fi | pet tracker | consumed | `plugins/barkinglabs_fi.py` (new) | 3 service UUIDs `57b40001/0210/3001-…` → role tagging (collar / base_config / base_proxy) |
| 10 | bosch-toolbox2 | power tools | consumed | `plugins/bosch_toolbox.py` (new) | 8 service UUIDs mapped to category (power_tool/measuring_tool/floodlight) + family (COMO 1.0/1.1/2.0, GCL, MIRX, Helios, EOS); byte-level tool-state decode deferred (needs raw scan record) |
| 11 | bose-bosemusic | audio | consumed | `plugins/bose.py` | **Fixed wrong IDs**: company `0x0065` (Lenovo)→`0x009E`, removed junk `0x3703`; service UUID `fe78` (Garmin)→`febe`; service data `fdf7` (Weber)→`febe`. Added BMAP UUID + `^Bose ` name matcher + `model_hint` metadata. Updated 2 legacy test files locked to wrong constants. |
| 12 | chipolo-net-v3 | tracker tag | consumed | `plugins/chipolo.py` | added `fe65` (current) + `fd44` (FMDN) service UUIDs; variant tagging (legacy/current/fmdn_a/fmdn_b/fmdn); 8-byte FMDN prefix detection + rotating-ID extraction; exported `CHIPOLO_PROXIMITY_UUID` for downstream iBeacon enrichment |
| 13 | concept2-ergdata | rowing machine | consumed | `plugins/concept2_pm5.py` (new) | name contains `PM5` or starts `Concept2`; passive report notes app uses legacy scan API so no byte parsing available |
| 14 | dexcom-g6 | CGM | consumed | `plugins/dexcom_cgm.py` | **Fixed wrong UUID** (was `61ce1c20-...`, now SIG `febc`); G6 name regex `^Dexcom[A-Z0-9]{2}$`; serial-tail extraction → identity hash `dexcom_g6:<XX>` |
| 15 | dexcom-g7 | CGM | consumed | `plugins/dexcom_cgm.py` | G7 community UUID `f8083532-...`; name prefix `DXCM`; model detection from UUID or name |
| 16 | ecobee-athenamobile | thermostat | consumed | `plugins/ecobee.py` (new) | `ecobee Inc. - <serial>` name pattern; serial-prefix → product family (61/63 thermostat, 71 THEIA camera, 72 HECATE contact) |
| 17 | embertech | smart mug | consumed | `plugins/ember_mug.py` | added vendor service UUIDs (`fc543621-…` original, `fc543622-…` ceramic); generation tagging; Nordic DFU UUID detection |
| 18 | evehome-eve | HomeKit accessories | covered | `plugins/matter.py` | Eve delegates BLE to Google Home APIs; existing matter plugin handles the `fff6` commissioning ads. Eve VID = 0x131D per CSA DCL registry (not currently mapped in matter plugin — future enrichment) |
| 19 | fitbit-fitbitmobile | wearable | consumed | `plugins/fitbit.py` | added primary service UUIDs (`fd62` SIG, `abbaff00-…` Gattlink, `26f33a00-…` Aria); product-line tagging from name; kept speculative Qualcomm-0x000A mfr path as best-effort |
| 20 | fixdapp-two | OBD-II scanner | consumed | `plugins/fixd_obd2.py` | **identification shifted to MAC-prefix list** per Android app: 7 OUI/prefix rules → `sensor_model` (OLD_KICKSTARTER / SETOSMART / VIECAR / VIECAR_V2); dropped name-required gate |
| 21 | flir-tools | thermal imager | consumed | `plugins/flir_tools.py` (new) | company_id 0x0AE9 (FLIR One) + Meterlink UUID + name prefix; byte-level decode deferred (native lib) |
| 22 | fluke-deviceapp | multimeter | consumed | `plugins/fluke.py` (new) | Fluke UUID base `b698XXXX-7562-11e2-b50d-00163e46f8fe` regex + name `^Fluke`; instrument-family code from variable XXXX |
| 23 | freestylelibre3-app-us | CGM | consumed | `plugins/freestyle_libre3.py` (new) | Abbott Libre 3 UUID base `0898XXXX-ef89-11e9-81b4-2a2ae2dbcce4` + name regex (`FreeStyle Libre 3`/`ABBOTT`/`LIBRE3`); limited passive surface since app uses direct-connect-by-MAC |
| 24 | gardena-smartgarden | irrigation | skip | — | Capacitor/hybrid app delegates BLE to remote web bundle (`bff-api.sg.dss.husqvarnagroup.net`); no BLE constants in APK. Re-visit if remote bundle is fetched |
| 25 | garmin-apps-connectmobile | wearable | consumed | `plugins/garmin.py` | added modern (`fe1f`) + legacy (`00001001-7791-…`) service UUIDs; 23-family name-prefix regex (Forerunner/fenix/Edge/vivo/venu/…); device-class refinement (HR monitor, power meter, satellite messenger) |
| 26 | govee-home | sensors/lights | consumed | `plugins/govee.py`, `plugins/govee_led.py` | added `ec88` service UUID to govee sensor plugin for UUID-only detection; existing rich mfr-data parsing (H5074/H5075/H5103/H5177/H5181/H512x) preserved |
| 27 | greenworks-tools | outdoor tools | consumed | `plugins/greenworks.py` (new) | unregistered company_id 0x15A8 + name regex (greenworks/gwelite/cramer) + MAC OUIs (34:12:/45:09:/A8:15:); brand tagging |
| 28 | huahuacaocao-flowercare | plant sensor | covered | `plugins/mibeacon.py` | Xiaomi HHCC uses MiBeacon service UUID `0xFE95` — already handled by our mibeacon plugin (product IDs 0x0098/0x015D/0x03BC/0x03BD) |
| 29 | husqvarna-automowerconnect | robotic mower | consumed | `plugins/husqvarna.py` (new) | service UUID `98bd0001-…` + company_id 0x0426; full TLV parser (length-type-value stream) extracts serial_number, state (9 values), activity (7 values), device_group (6 values), pairing flag; **rich live operational telemetry** |
| 30 | insulet-myblue-pdm | insulin pump | consumed | `plugins/insulet_omnipod.py` (new) | name-based detection only (TWI SDK uses dynamically-resolved company ID and native byte parsing — not recoverable from Java) |
| 31 | irobot-home | robot vacuum | consumed | `plugins/irobot.py` (new) | name regex (Altadena/iRobot Braav/Roomba) + service UUID `0bd51777-…` + mfr-data magic `A8 01 ?? 31 10`; disambiguates from Mammotion which shares company_id 0x01A8. **2026-04-27**: added 2nd service UUID `c74edd21-763c-4e54-85a8-43bb75035d75` (Robot Control Command — present in `libcore_jni.so` strings) after live capture |
| 32 | kinsa-polaris-app | smart thermometer | consumed | `plugins/kinsa.py` (new) | UUID regex `^[0-9a-f]{8}-[0-9a-f]{4}-746c-6165-4861736e694b$` (ASCII "Kinsa Health" fragment) + name prefixes (Kinsa/AViTA/KS_) |
| 33 | kwikset-blewifi | smart lock | consumed | `plugins/kwikset.py` (new) | 4 variants (consumer/auraReach/halo3/multifamily) with different mfr-data byte offsets; 9-byte `unique_id` + product_id + protocol_version + notification indices; halo3 exposes live `lock_status_info` (locked/unlocked/jammed) |
| 34 | lifx-lifx | smart bulb | skip | — | app version in this batch has no BLE (WiFi-only setup) — no BLE-layer advertisement exists to parse |
| 35 | masimo-merlin-consumer | pulse ox | consumed | `plugins/masimo.py` (new) | company_id 0x0243 + `MightySat`/`Masimo` name; exposes protocol_version byte (offsets inside mfr data obfuscated, not decoded) |
| 36 | maytronics-app | pool cleaner | consumed | `plugins/maytronics.py` (new) | multi-path name regex (IoT_PWS/maytronics00/MxX_pws/bare 8-char serial/12-char hex) + 6-byte mfr-data layout (model_code/proto/serial-hash/mu-version) |
| 37 | milwaukeetool-mymilwaukee | power tools | consumed | `plugins/milwaukee_onekey.py` (new) | company_id 0x0604 + SIG UUID `fdf5` + legacy 128-bit UUID; raw payload hex exposed (field-level decode not recoverable from APK) |
| 38 | nuki | smart lock | consumed | `plugins/nuki.py` (new) | 9 service UUIDs across 5 product families (lock/bridge/fob/box/keypad); role/state tagging (advertising/pairing/keyturner/firmware_update); pairing-mode detection |
| 39 | orbit-smarthome | irrigation | consumed | `plugins/orbit_bhyve.py` (new) | name regex `^bhyve_[0-9a-f]{6}$` + vendor-UUID-base pattern `*-fe32-4f58-8b78-98e42b2c047f`; device-id suffix as identity |
| 40 | pg-oralb-oralbapp | toothbrush | consumed | `plugins/oralb.py` | **Fixed off-by-2 byte layout**: fields were shifted — "state/pressure/minutes/seconds" were actually deviceType/softwareVersion/deviceState/status. Correct layout: protocolVersion(0) deviceType(1) softwareVersion(2) deviceState(3) statusByte(4) brushTimeMin(5) brushTimeSec(6). Added 37-model DEVICE_TYPES table, full 13-state DEVICE_STATES, status-byte bit unpack (pressure/power-btn/mode-btn) |
| 41 | philips-lighting-hue2 | smart bulb | skip | — | passive report says the `com.philips.lighting.hue2` app has no BLE functionality at all; Hue bulbs would be handled by the separate `com.signify.hue.blue` app which wasn't processed |
| 42 | polar-polarflow | HR/wearable | consumed | `plugins/polar.py` (new) | company_id 0x006B + service UUID `feee` + name regex; PbMasterIdentifierBroadcast parse (SF byte validation, user_id_len, reversed-BigInteger Flow user ID); payload-length tier hint (13=watch, 11=strap); GoPro-paired flag |
| 43 | positec-landroid | robotic mower | consumed | `plugins/worx_landroid.py` (new) | service UUID `abf0` only; state behind GATT — passive scanner detects presence, not state |
| 44 | rachio-iro | irrigation | skip | — | Rachio controllers are WiFi-only; no BLE radio. Matter code in APK is vendored-dead |
| 45 | rainbird-rainbird2 | irrigation | consumed | `plugins/rainbird.py` (new) | 3 detection paths: name contains RAINBIRD (LNK2/RC2), regex `^BAT-(BT\|PRO)-(\d+)I?$` (ESP-BAT with zone count), Solem 128-bit UUIDs |
| 46 | rivian-android-consumer | vehicle/phone key | consumed | `plugins/rivian.py` | already correct — company ID 0x0941 matches; passive report confirms obfuscated byte layout, no additions safely derivable |
| 47 | segway-mower | robotic mower | consumed | `plugins/segway_navimow.py` (new) | unregistered company_id 0x4E42 (ASCII "NB"); BLE type byte (0xE0 mower / 0x23 MowGate), protocol-version ≥ 2 gate, 1's-complement checksum validation; FindMy variant exposes serial in cleartext |
| 48 | sphero-sprk | robot toy | consumed | `plugins/sphero.py` | added 6-model prefix map (BB=BB-8, GB=BB-9E, SK=SPRK+, SB=BOLT, SM=Mini, RV=RVR); added v1 (`22bb746f-…`) + v2 initializer (`00020001-574f-…`) service UUIDs; stable identity hash via model+device_id |
| 49 | swordhealth-guarda | health wearable | skip | — | Passive report shows the app only filters on generic `0xFFF0` (JStyle SDK) which many unrelated BLE devices use; registering would cause false positives with no Sword-specific signal to disambiguate |
| 50 | teslamotors-tesla | vehicle/phone key | consumed | `plugins/tesla.py` (new) | service UUID 0x1122 + `S.{3,}` name filter; model char at index 3 (3/Y/S/X/C/R/D/P); VIN-hash fragment = name[3:] used as stable identity. **2026-04-27**: added CID `0x022B` (Tesla, Inc.) match for non-vehicle product (Powerwall / Wall Connector / accessory candidate) — vehicle keys don't use mfr-data, so a CID hit means a different Tesla product; tagged `device_class=tesla_product` until further captures pin it down |
| 51 | thetileapp-tile | tracker tag | consumed | `plugins/tile.py` | added `feec` (PrivateID / current) alongside `feed` (legacy); variant tagging (legacy/privateid); exact byte-offset parsing of hashedId/counter/tx_power deferred (PairIP-obfuscated in APK) |
| 52 | tpky-mc | Tapkey Mobile lock | consumed | `plugins/tapkey.py` (new) | **vendor identified: Tapkey (not TP-Link)** — service UUID `6e65742e-7470-6b79-2ea0-000006060101` (ASCII "net.tpky.") + mfr-data magic byte `(magic & 0x7F)==1`; bit 7 = lockId-incomplete flag |
| 53 | tractive-android-gps | pet tracker | consumed | `plugins/tractive.py` (new) | 4 per-family UUIDs (Cat/Dog/V2/Fw4); 12-name DFU-mode detection (TRDOG1/TRCAT1/TG4410/TG5/TG6A/etc.) with `dfu_model_code` metadata |
| 54 | traegergrills-app | smart grill | consumed | `plugins/traeger.py` (new) | name prefix `Yosemite` + provisioning UUID `a8220000-…`; model-code decode from raw scan record deferred (needs raw bytes not in RawAdvertisement) |
| 55 | volvo-vcc | vehicle/phone key | consumed | `plugins/volvo.py` (new) | Volvo uses Apple iBeacon wrapper (company_id 0x004C) with fleet-wide proximity UUID `e20a39f4-73f5-4bc4-1864-17d1ad07a962`; major/minor (big-endian) carry per-vehicle SKI fragment → used as stable identity |
| 56 | withings-wiscale2 | smart scale | consumed | `plugins/withings.py` (new) | SIG UUIDs 0x9990-0x9999 mapped to 5 product models (scales/BPM/etc.); "WITH" (0x5749 0x5448) marker detection on custom UUIDs; manufacturer-data MAC → provisioning state (unprovisioned/provisioning/paired); name-prefix matchers (bl_hwa, WSM02, WBS0x, WPM0x) |
| 57 | combustion-app | smart thermometer | consumed | `plugins/combustion.py` (new) | service UUID `00000100-CAAB-…` + company_id `0x09C7`; full Probe (21B) decode (serial + 8x13-bit packed temperatures via `T = raw*0.05 - 20`°C + mode/color/probe_id + battery_low + hop_count); Gauge (19B) decode (ASCII serial + temp + alarms); DFU mode via name-prefix regex (`Thermom_DFU_`/`Display_DFU_`/`Charger_DFU_`/`Gauge_DFU_` + 3 legacy `CI * BL` names) |
| 58 | candyhouse-sesame2 | smart lock | consumed | `plugins/candyhouse_sesame.py` (new) | service UUID `0xFD81`; full 34-model `CHProductModel` enum (SS2/3/4/5/5PRO/Bot/Touch/Hub3/Face/etc.); per-family device-ID slice (Hub3 bytes[2..9] 8B, WM2 bytes[3..11] 9B, OS3 bytes[3..18] 16B, OS2 SS2/4/Bot1/BiKeLock1 base64-decoded local_name); registration-bit + adv_tag_b1 decoded; identity hash uses stable in-advert device-ID |
| 59 | mopeka-tankcheck | tank level sensor | rewritten | `plugins/mopeka.py` (existed) | major rewrite from fictional layout — service UUIDs `0xFEE5` (nRF52), `0xADA0` (CC2540), gateway `7F330000-…`; full nRF52 10B decode (8 hardware variants from `Kr` enum, battery `(b&0x7F)/32`V, temperature `(b&0x7F)-40`°C with sync-button bit, 14+2 bit level+quality with extended-range support, 3B MAC tail, signed accelerometer X/Y); CC2540 path (accel-LUT, BMPro quality, battery formula, temp `1.776964*(raw-25)`, slow-update + sync flags, 0xAA/0xFF-0x3F sentinels, raw echo list); identity uses in-payload MAC tail when present |
| 60 | weber-igrill | thermometer/grill | enriched | `plugins/weber_igrill.py` (existed) | added Weber company_id `0x043F`, all 6 per-model service UUIDs (KT Mini, KT, iGrill_Mini v1/v2, iGrill_v2 B2, Pulse), and name prefixes `kt_`/`kt$`/`pulse \d+`. Model table now covers Kitchen Thermometer + Pulse families |
| 61 | jbl-stc-com | speaker/headphone | enriched | `plugins/jbl.py` (existed) | added Harman company_id `0x0ECB` matching, Avnera-silicon service UUID `58622534-…`, byte-level PID extraction (BE uint16 at payload[0..1]) and BD_ADDR extraction (payload[2..7]) from both mfr-data and FDDF service-data. Identity hash now uses embedded BD_ADDR when available (survives BLE random-address rotation) |
| 62 | hatchbaby-rest | nightlight/sound | enriched | `plugins/hatch.py` (existed) | added vendor service UUIDs `02240000-…` (gen1) and `02260000-…` (gen2) to discovery, `^Hatch ` name prefix, generation tag from matched UUID, Nordic LegacyDFU UUID `00001530-…` flips `dfu_mode`. Existing CID `0x0434` byte-level decode preserved |
| 63 | sphero-spheromini | toy robot | enriched | `plugins/sphero.py` (existed) | added Mini-specific service UUID `258EAFA5-E914-47DA-95CA-C5AB0DC85B11`; UUID-only match auto-tags `model="Mini"`. SM- prefix already handled by existing 6-model name regex |
| 64 | dyson-mobile-android | vacuum/purifier/hair-care | consumed | `plugins/dyson.py` (new) | service UUID `2DD10010-…` (factory-state) tagged `state=unprovisioned`; company_id `0x0A12` with prefix `0x01 0x01` tagged `state=provisioned_beacon`; bytes 4+ exposed as `state_bytes_hex` for downstream RE; `^Dyson ` extracts model_hint |
| 65 | dewalt-toolconnect | power tools/batteries | consumed | `plugins/dewalt.py` (new) | service UUID `0xFACE` + DeWalt CID prefix `FE 00` (`0x00FE` LE). Division-byte dispatch (battery/light/drill/compact_light). Battery decode: SoC% (4-bit step), 16 status flags (LSB+MSB), firmware major/minor, temperature `b-40`°C, voltage `b/10`V, usage coulombs, impedance, capacity, voltage-health, 24-bit big-endian PTI from `(payload[16] payload[17] payload[9])` |
| 66 | tedee-mobile | smart lock | consumed | `plugins/tedee.py` (new) | Tedee vendor UUID base regex `^[0-9a-f]{8}-4899-489f-a301-fbee544b1db0$` extracts `tedee_service_handle_hex`; `^Tedee( |$)` name extracts model_hint; Nordic Secure DFU `0000FE59-…` flips `dfu_mode` only when paired with a Tedee signal (avoids false positives). Identity hash uses Tedee service handle |
| 67 | lemurmonitors-bluedriver | OBD-II dongle | consumed | `plugins/lemur_bluedriver.py` (new) | service UUID `a9da6040-…` + name regex `(?i)^(BlueDriver|VHC)`; product_family tagged from name (bluedriver/vhc) or unknown for UUID-only |
| 68 | nanoleaf-nanoleaf | smart light (HomeKit) | consumed | `plugins/nanoleaf.py` (new) | Apple HomeKit company_id `0x004C` (gated on first payload byte `0x06` = HAP AD type) + Nanoleaf company_id `0x080B` + name regex. Full HAP § 7.4.2.2 decode: AD version, length, category enum, GSN counter, 6-byte AAI (stable per-pairing identifier — used as identity hash), config number, compatible version. Model family from name |
| 69 | eufylife-smarthome | scale/lock/vacuum | skip | — | packed outer DEX (s.h.e.l.l.A AppComponentFactory + JNI classloader), Hermes JS bytecode, native `libBleLib.so` with UUIDs constructed programmatically (no .rodata literals). Nothing statically recoverable. Re-visit after Frida dump or live capture |
| 70 | ouraring-oura | smart ring | consumed | `plugins/oura.py` (new) | Oura company_id `0x02B2` + 3 service UUIDs (data + charger puck + Cypress DFU); mfr-data nibble unpack: payload[1]=mode/hwtype (5-value enum), payload[2]=i_nibble/color, payload[3]=design code; charger UUID auto-tags `device_kind=charger_puck`, DFU UUID flags `mode=BOOTLOADER` |
| 71 | whoop-android | fitness strap | consumed | `plugins/whoop.py` (new) | three generation-specific 128-bit UUIDs (Gen4 / 4.0 Maverick / Puffin) + `^WHOOP` name. Generation tagged from matched UUID; serial extracted from `^WHOOP[ _-]?<hex>` name; identity hash uses serial when present |
| 72 | beurer-healthmanager | medical (multi) | consumed | `plugins/beurer.py` (new) | Beurer company_id `0x043A` + 6 vendor UUIDs + name regex covering 8 product-line prefixes (BM/BC/BF/AS/GL/PO/rebrand). Product family classified from name. Scale mfr-data MAC extraction: walks for `0xFF` sentinel, skips 4 bytes, reads 6-byte little-endian MAC, byte-reverses for display |
| 73 | interaxon-muse | EEG headband | consumed | `plugins/muse.py` (new) | SIG service UUID `0xFE8D` + InteraXon company_id `0x025E` + name regex `^MuseS?-`. Generation decoded from mfr byte 1 (5/6/7 = MuseS); MAC suffix from name = persistent identifier, used for identity hash |
| 74 | assaabloy-mobileaccess | enterprise access | consumed | `plugins/assa_abloy_origo.py` (new) | HID Global company_id `0x012E` + Apple `0x004C` (gated on iBeacon-with-Origo-UUID). Per-deployment UUID prefix regex extracts 24-bit deployment code. Full HID mfr decode (protocol/profile versions, opening-type bitmask, credential-unavailable bit, 4 RSSI thresholds). iBeacon phone-broadcast decode (deployment-code, 4-byte major/minor as stable per-enrollment credential ref → identity basis, TX power) |
| 75 | omronhealthcare-omronconnect | medical (BP/scale/glucose) | consumed | `plugins/omron.py` (new) | Omron CIDs `0x020E`+`0x0105` + 6 SIG UUIDs + custom oximeter UUID + name regex `^BLE[sS]mart_`. Product class auto-tagged. Name decode (4-hex model + 4-hex subtype + serial_tail). Full EachUserData mfr decode: numUsers 1-4, time_not_set, pairing_mode, bluetooth_standard_mode flags, per-user (last_sequence_number, number_of_records). Identity uses serial-tail |
| 76 | senseonics-gen12androidapp | implanted CGM | consumed | `plugins/eversense_cgm.py` (new) | service UUID `c3230001-…` only — pure presence-only class-III medical device beacon |
| 77 | tandemdiabetes-tconnect | insulin pump + CGM | consumed | `plugins/tandem_pump.py` (new) | SIG `0xFDFB` (Tandem t:slim X2) + `0xFDE3` (Abbott Libre 3) + `^ABBOTT` name. device_kind tagging (tslim_x2_pump/libre3_cgm); both UUIDs → `therapy_mode=hybrid_closed_loop`. Libre 3 sensor serial extracted from name (stable for 14-day lifespan). All matches flagged `safety_critical=True` |
| 78 | bose-monet | audio | enriched | `plugins/bose.py` (existed) | added 2 service UUIDs (SIG `0xFDD2` + vendor `F4C93A79-…`); mfr-data variant byte decode (0x9E=120-series, 0x03=104-series, 0x10/0x00/0x01=legacy, 0x41=Bragi); 5-byte partial MAC hash extracted from offsets 4-8 and used as identity-hash basis (survives BLE random-address rotation) |
| 79 | oticon-app | hearing aids | consumed | `plugins/oticon.py` (new) | heuristic plugin (NativeAOT prevented full UUID recovery from APK): Demant CID `0x01D7` + ASHA UUID `0xFDF0` + name regex `^Oticon `. ASHA-only matches → `vendor_attribution=uncertain`; name or CID matches → `vendor_attribution=oticon`. Model + L/R side extracted from name |
| 80 | sonova-phonak-dsapp | hearing aids | consumed | `plugins/phonak.py` (new) | Sonova company_id `0x0282` + name regex covering Audeo/Audéo/Naída/Virto/Sky/Phonak product lines. Mfr-data payload preserved as hex (decode in libMobileCoreJni.so). Product line tagged from name |
| 81 | mysugr-android-companion | diabetes companion | skip-covered | — | mySugr is a multi-vendor consumer of standard SIG profiles (`0x1808` Glucose / `0x1810` BP / `0x181D` Weight) — no proprietary advertising shape. Defer to a future generic SIG-medical-profile plugin |
| 82 | lifescan-reveal | glucose meter | skip-covered | — | LifeScan UUIDs runtime-decrypted from libstring-provider.so; only static signal is SIG Glucose `0x1808` (vendor-agnostic). Same situation as mysugr |
| 83 | viatom-vihealth | medical (multi, 226 models) | consumed | `plugins/wellue_viatom.py` (new) | 6 vendor UUIDs (Wellue primary/secondary + 4 Vtrump variants) + name substring catalog covering 33 most-discriminative product prefixes from 226-name roster. Product class auto-tagged (pulse_ox_ring/ECG patch/BP/scale/pediatric/fetal_HR/etc.). MAC OUI shortcuts `:4D:57` and `:56:10`. Vtrump iBeacon UUID flag |
| 84 | tplink-iot | Matter ecosystem | enriched | `plugins/matter.py` (existed) | **fixed bug** where parser only accepted opcode `0x01` (extended/reserved) and rejected canonical `0x00` "Commissionable" used by most Matter devices including TP-Link Tapo. Now accepts both, tags `opcode_label` in metadata |
| 85 | audiotechnica-connect | audio | consumed | `plugins/audio_technica.py` (new) | AT-proprietary Airoha UUID `DC7783C4-…` (strong AT fingerprint) + name regex `^ATH-`. Chipset family auto-tagged (airoha/qualcomm_qcc/unknown) |
| 86 | sennheiser-control | audio | consumed | `plugins/sennheiser.py` (new) | Sennheiser CID `0x0494` + Sonova CID `0x0BA3` + 3 service UUIDs (Sennheiser `0xFDCE`, Sonova `0xFCFE`, AMBEO POPCORN) + name regex (Momentum/CX/HD/IE/AMBEO/Sennheiser). Brand auto-tagged. AMBEO UUID flips wifi_setup_mode. `LE-`/`LE ` name prefix stripped per the bundle's regex. Mfr-data preserved as opaque hex (TLV decode in Hermes JS) |
| 87 | jabra-moments | audio | consumed | `plugins/jabra.py` (new) | service UUID `0xFEFF` (GN Netcom) + name `^Jabra `. Full 8-byte mfr decode: serv_type_beacon, product_id u16le (mapped to Evolve/Engage/Talk), unique_id u32le (persistent per-unit, used as identity), TX power |
| 88 | nothing-smartcenter | audio | consumed | `plugins/nothing.py` (new) | three Nothing CIDs (`0x0CCB`/`0xCB0C`/`0xFFFF`) + Fast Pair UUID `0xFE2C` + name regex `^(Nothing|CMF )`. Persistent device-ID = last 6 bytes of mfr-data (identity hash basis). Fast Pair short form extracts 3B model_id; ≥4B = account_key_filter. Both signals = high_confidence. Fast Pair alone gated to avoid false positives |
| 89 | bmw-connected | tracker tag (BMW-branded) | consumed | `plugins/bmw_bury_findmate.py` (new) | only the BMW-branded Bury Find Mate tracker is BLE-visible (vehicle uses Classic BT + NFC). Exact local-name match `BMW FMT` (factory/unregistered) or `BMW FM1` (registered). State auto-tagged |
| 90 | sony-songpal-mdr | audio | enriched | `plugins/sony_audio.py` (existed) | added Apple CID `0x004C` matching for Sony pseudo-iBeacon (proximity UUID `00000000-7A46-1001-B000-001C4D2CA2D7`). Family tag at payload[2..3] decoded (audio `0x0004` / lighting `0x000A`). Subtype byte surfaced. Sony iBeacon major/minor used as identity hash basis |
| 91 | oceanwing-soundcore | audio | consumed | `plugins/soundcore.py` (new) | best-effort (Flutter Dart AOT not statically recoverable). Anker company_id `0x07AB` + name regex `^[Ss]oundcore `. First payload byte stored as `product_code_byte` for downstream RE |
| 92 | stationdm-bluelink | vehicle (Hyundai) | skip-no-ble | — | Bluelink app delegates all Digital-Key BLE to Google Play Services CCC framework; vendor-side Java has zero BLE code. Vehicle-side shape would require live capture |
| 93 | stationdm-genesis | vehicle (Genesis) | skip-no-ble | — | same as Hyundai Bluelink — app commercially packed, all CCC delegation to Google/Samsung DKey, vendor Java has zero BLE code |
| 94 | anki-cozmo20 | robot toy (Vector) | consumed | `plugins/anki_vector.py` (new) | service UUID `0xFEE3` (Anki Inc.) + name regex `^Vector-[A-Z0-9]{4}$`. ESN suffix extracted from name as stable identity-hash basis (`anki_vector:<XXXX>`); presence of beacon implies `pairing_mode=True` since Vector only advertises during the back-button-double-tap pairing window. No mfr-data / service-data |
| 95 | breville-connectedcoffee | appliance (espresso/oven/sous-vide) | consumed | `plugins/breville.py` (new) + `plugins/acaia_scale.py` (enriched) | dual-CID dispatch: modern Breville `0x0955` (version + 8-model byte map CSJ100/BOV950/BSV600/BES995/BES1010/BEA900/BES87x/LUNAR + 4-byte unique appliance ID) and legacy ChefSteps Joule `0x0159` (type + family-prefix `A0 A3 5D B7 E1` + 3-byte device variant). Joule service UUID `700B4321-…` adds UUID-only match path. Identity uses unique-ID-hex (modern) or device-variant-hex (Joule). Side-effect enrichment: added Acaia "BB" sentinel CID `0x4242` to `acaia_scale.py` (CID-only matches now tag `bb_sentinel=True`) |
| 96 | chefsteps-circulator | sous-vide (Joule) | consumed | `plugins/breville.py` | same plugin: Joule path matches CID `0x0159` OR `700B4321-…` UUID; verifies family prefix before extracting variant |
| 97 | breville-connect | smart-home (Tuya white-label) | skip-covered | — | Tuya `0xFD50` + CID `0x07D0` already handled by `tuya.py`; Matter `FFF6` handled by `matter.py`. No Breville-specific signal |
| 98 | breville-cookingapp | appliance (Joule + ovens) | skip-opaque | — | proprietary `expo.modules.blekit` native module absent from base APK; only model-byte enum is statically visible (and conflicts with the connectedcoffee table at indices 5/6). connectedcoffee already covers the recoverable Breville passive surface |
| 99 | fellowapp | kettle (Stagg/Corvo) | consumed | `plugins/fellow.py` (new) | Fellow primary service UUID `2291C4B6-…` + aux UUID `7AEBF330-…` + name regex `^(Stagg EKG Pro\|Corvo EKG\|Fellow EKG Pro)`. Model auto-tagged from name; ESP32 `<name>-<HEXSUFFIX>` tail extracted as `mac_suffix` and used as identity-hash basis. `aux_service_seen=True` flag when the OTA/prov UUID is present |
| 100 | panasonic-technicsaudioconnect | audio (EAH-AZ earbuds) | consumed | `plugins/panasonic_technics.py` (new) | Panasonic CID `0x003A` + Airoha service UUIDs (`5052494D-…` primary, `49535343-…` TRSPX) + name regex `^(EAH-\|Technics EAH-\|LE-EAH-)`. Decodes `payload[4]=model_byte` and `payload[5..10]=BR/EDR MAC` (the durable Classic-BT address leaked in the BLE advert — used as identity-hash basis). Name regex extracts model token and `technics_branded` / `le_only_variant` flags |
| 101 | resmed-airmini | CPAP (BR/EDR-only) | skip-no-ble | — | AirMini transport is BR/EDR (RFCOMM/SPP) only; no BLE advertisement layer to scan. The myAir companion report covers AirSense/AirCurve BLE families that *do* advertise |
| 102 | resmed-myair | CPAP / sleep-test / oxygen | enriched | `plugins/resmed.py` (existed) | added AirMini/AS11/AirCurve name regex (`^(AirMini\|AS11\|AirCurve)-[A-Z0-9]{10,14}$`) with serial extraction → identity-hash basis; NightOwl service UUID `4D521000-…` + name prefix → `device_class=sleep_test`; POC service UUID `D2009798-…` → `device_class=oxygen_concentrator`. Existing FD56 + CID `0x038D` + `^ResMed\\s` paths preserved |
| 103 | qingniu-renpho | scale (Qingniu OEM family) | enriched | `plugins/renpho.py` (existed) | added Qingniu CID `0x0157` alongside legacy `0x06D0`; full 29-name OEM catalog (Yolanda-CS*, JiaHua/Dretec/Wbird/Sunnyway/JiaBao/Beryl, QN-Scale/QN-Scale1/QN-WristBand) with longest-prefix-wins → `brand`+`model_code`; UUIDs `FFF0/FFE0/ABF0/181D`; live-weight decode under Qingniu CID (LE16 in 100g units, unit nibble, stable bit, embedded 6-byte MAC tail used as identity when present) |
| 104 | googleadm | tracker (FMDN) | skip-tampered | — | Aptoide-served APK was a repackaged dance-tips app, not the real Find My Device client. Genuine FMDN spec is already covered by `eddystone.py` (`0xFEAA`), `chipolo.py` (`0xFD44`), and `google_fmd.py` (`0xFE2C`). No new plugin work warranted |
| 105 | theswitchbot-switchbot | smart-home (multi) | covered | `plugins/switchbot.py` (existed) | service UUID `0xFD3D` + CID `0x0969` + per-device-type byte decode already implemented for 12 device types. Report flags byte-map discrepancies vs the public OpenWonderLabs SwitchBotAPI-BLE doc (e.g. `0x77` Contact vs hub2 in plugin) — reconciliation against live captures is a follow-up |
| 106 | medtronic-diabetes-minimedmobile-eu | insulin pump (UI shell) | skip-opaque | — | UI-shell APK; all BLE code is in the sibling `com.medtronic.minimed.ngp` service which isn't in the download set. Zero BluetoothGatt / BluetoothLeScanner refs and no `BLUETOOTH_SCAN` permission |
| 107 | simplisafe-mobile | security (base + lock) | consumed | `plugins/simplisafe.py` (new) | SimpliSafe CID `0x06B1` (1713) + base-station UUIDs `0x0D18`/`0x00CC` + lock-app UUID `26526EEA-…`. Nordic DFU `0xFE59` gated to also require the SimpliSafe CID. Decodes 4-byte `serial_tag` (SHA-256 fragment of serial — privacy-preserving) and `extended_serial_tag`; identity hash uses serial_tag. Product class auto-tagged base_station / smart_lock; `mode=application|dfu` for locks |
| 108 | pebblebee-app-hive3 | tracker tag | consumed | `plugins/pebblebee.py` (new) | three-family dispatch: Honey (exact local-name `PebbleBee`), Finder (CID `0x0B1E`/2846 + service UUID `0xFCA5`), R4K Tag/R4K-FM (CID `0x09DA`/2522 + service UUID `0xFCC7`). Type discriminator byte and `state_bytes_hex` exposed; bit-level decode lives in libapp.so. R4K-FM's co-advertised Apple Find My frame is handled by the existing apple_findmy parser |
| 109 | sonos-acr2 | audio (speakers + Ace headphones) | enriched | `plugins/sonos.py` (existed) | added SIG service UUID `0xFE07` (the report's primary discovery filter) and `^Sonos ` name pattern. UUID-only matches surface `match_source=service_uuid`; existing CID `0x05A7` payload parse preserved. Privacy note from report: advertised name is the user-set room label — surfaced as `device_name` but downstream operators should avoid uploading it |
| 110 | sengled-life3 | smart-lighting (BLE Mesh + Matter) | skip-covered | — | Sengled discovery is SIG Mesh `0x1827` (provisioning) + `0x1828` (proxy), already handled by `plugins/bt_mesh.py`. Matter-over-BLE `0xFFF6` is handled by `plugins/matter.py`. No Sengled-specific advertisement shape that isn't already covered |
| 111 | honeywell-android-lyric | thermostat (commissioning beacon) | consumed | `plugins/honeywell_lyric.py` (new) | exact `^HON_NP` name prefix + service UUID `8821813E-…`. Trailing serial extracted as `serial_suffix` and used as identity-hash basis. Presence of beacon implies `unprovisioned=True` (Lyric stops advertising once Wi-Fi-paired) |
| 112 | homeassistant-companion-android | smart-home (central + iBeacon TX) | skip-no-ble | `plugins/improv_wifi.py` (new, side-effect) | HA itself is a central scanner / iBeacon TX, not a peripheral. iBeacon scan path covered by existing altbeacon/apple_proximity. Side-effect: created `improv_wifi.py` for the Improv UUID `00467768-…` HA scans for — catches any ESP32/ESPHome device in Wi-Fi-provisioning mode |
| 113 | adsmart-thermbbq | thermometer (BBQ probes) | consumed | `plugins/thermopro_bbq.py` (new) | three chipset-family service UUIDs (`0B863E05-…` fff, `A3C3A551-…` a3c3a55, `1086FFF0-…` 1086fff) → `chipset_family` tag; name regex `^(Thermopro\|TP\\d{2,3}\|P900\|YKE-[A-Z]\\d?-DFU)$` covers TP9xx/TP25/P900 + YKE-*-DFU bootloader aliases. Distinct plugin from existing `thermopro.py` (different product family — BBQ probes have no mfr-data telemetry; existing plugin parses indoor TP358-style ads) |
| 114 | napoleon-accuprobe | thermometer (BBQ) | consumed | `plugins/napoleon_accuprobe.py` (new) | service UUID `0xFF00` + name substring `NAP_KT` (V1) or `ACCU-PROBE` (V2). Confidence tagged `low` for UUID-only (0xFF00 is a commodity-module SIG), `medium` name-only, `high` both. Product family auto-tagged |
| 115 | kef-connect | audio (premium speakers) | consumed | `plugins/kef.py` (new) | KEF vendor UUID `baf5aa64-…` + Qualcomm GAIA `00001100-d102-…` + Fast Pair `0xFC5D` + `^KEF ` name. 4-byte mfr-data decoded: `project_code` (model fingerprint, identity-hash basis) and 3 status bits (`is_power_on`/`is_connected_with_bonded_device`/`is_in_pairing_mode` — household-occupancy proxy flagged for privacy) |
| 116 | wahoofitness-fitness | fitness (sensors / trainers / cycling computers) | consumed | `plugins/wahoo.py` (new) | Wahoo CID `0x01FC` (508) + 6 proprietary UUIDs (`0xEE06` ELEMNT, `0xEE07` legacy gym, `0xEE0A` DFU, `0xEE0D` KICKR BIKE, `0xEE0E` treadmill, `0xEE0F` POWRLINK) + name regex catalog covering TICKR/KICKR/KICKR BIKE/ELEMNT/HEADWIND/POWRLINK/TRACKR/RUNNR/MIRROR/RFLKT/Dreadmill with 4-6 hex-char serial extraction → identity-hash basis. ELEMNT mfr-data → `product_id` + `hw_version` (uint16 LE pair). Generic SIG sensor UUIDs (HR/CSC/CPM/RSC/FTMS) intentionally NOT matched here to avoid stealing non-Wahoo sensor sightings |
| 117 | alivecor-kardia | medical (ECG) | enriched | `plugins/alivecor_ekg.py` (existed) | added KardiaMobile 6L UUID `AC060001-…` and KardiaCard UUID `AC010001-…` alongside legacy v1.0.0 UUID; name regex extended to `^(KardiaMobile\|KardiaCard\|EKG-)` with `KardiaMobile_6L_<serial>` / `KardiaCard_<serial>` → `product_family`+`device_id`. Identity hash now prefers device_id (survives MAC rotation) with mac fallback for UUID-only matches |
| 118 | edifier-connect | audio (earbuds / speakers) | skip-needs-product-db | — | per-SKU 16-bit UUIDs of form `0000XX00-…` (156 SKUs). Without importing the decrypted `assets/products_release.json` (key in CipherUtil.java:67-68), a partial-UUID match would collide with legitimate SIG-allocated UUIDs. Stage 6 follow-up |
| 119 | ledvance-smartplus | smart-lighting (BLE Mesh + HomeKit) | skip-covered | — | LEDVANCE SMART+ advertises as standard SIG-Mesh nodes (`0x1827`/`0x1828` already in `bt_mesh.py`); legacy Cypress HomeKit variant (Apple CID `0x004C` HAP) handled by `nanoleaf.py`. `LEDVANCE SMART+` name could enrich `bt_mesh.py` metadata but byte-level discovery is identical |
| 120 | tracki-pet | tracker (cellular GPS) | skip-opaque | — | Tracki app uses unfiltered BLE scan (null ScanFilter); no service UUID / mfr-data / name-prefix enforced statically. Privacy story is dominantly cellular (LTE-M/NB-IoT to Trackimo cloud). Re-visit after live capture |
| 121 | nespresso-activities | coffee (machine + Aeroccino) | enriched | `plugins/nespresso.py` (existed) | added Aeroccino UUID `65241910-…` alongside existing machine UUID `06AA1910-…`; relaxed parse() to accept UUID-only matches (the canonical signal — app filters by UUID alone, not by mfr-data). UUID match auto-tags `product_class=coffee_machine` or `aeroccino`; existing CID `0x2502` payload-decode preserved as fallback |
| 122 | chamberlain-android-liftmaster-myq | garage / camera / lock | consumed | `plugins/chamberlain_myq.py` (new) | three vendor UUIDs cover all three product families: CHUB `26d91a37-…` → `garage_hub`, Tend camera `0x1888` + `^Lynx` name → `tend_camera`, Lockitron legacy `A1A51A18-…` → `lockitron` (`legacy_product=True`). No advertisement-layer telemetry per the report — CHUB post-connect JSON is unbonded + cleartext (privacy concern) |
| 123 | eufy-security | security camera (packed) | skip-covered | — | Ijiami-packed; reachable signals (Tuya `0xFD50`, BLE-Mesh `0x1827/1828`) already covered. Conjectured `8c85xxxx-…` eufy-direct UUID is low-confidence (libapp.so strings only) — defer to a future Frida pass |
| 124 | sensibo-app | smart-AC controller | skip-needs-libapp-so | — | Flutter `libapp.so` missing from base APK; no static UUID/CID/name. Sensibo devices only advertise during the 1-5 min setup window (privacy-positive). Re-visit after XAPK fetch or live capture |
| 125 | schlage-mobileaccess | enterprise commercial locks (Allegion Engage) | consumed | `plugins/allegion_engage.py` (new) | distinct from `plugins/schlage.py` (residential). Allegion CID `0x013B` (the `[0x3B, 0x01]` sentinel interpreted as LE) + adv_version + BE16 device_type + 3-state byte. v3+ walks LTV protocol-block stream into `protocol_blocks` with ENGAGE/SENSE/SAPPHIRE/SIG_AUTH/IOT_NATIVE type names; per-block state + security_version + ENGAGE `dynamic_mtu_supported` flag. AD-type 0x10/0x16 serial extraction deferred (not in RawAdvertisement) |
| 126 | dewalt-sitemanager | power tools / smart batteries | covered | `plugins/dewalt.py` (existed) + `plugins/apple_proximity.py` | DEWALT `0xFACE` + CID `0x00FE` already covered by dewalt.py with full battery decode; iBeacon Smart Batteries handled by apple_proximity. DIS `0x180A` POWERSHIFT secondary filter is too generic to match alone |
| 127 | assaabloy-yaleconnect | smart-lock (LatAm Yale Connect) | enriched | `plugins/august_yale.py` (existed) | added Yale Connect 3-byte mfr-data decode under CID `0x012E`: protocol version (major.minor packed nibble), `joined` bit (paired vs factory-fresh), `encrypted` bit (AES channel active), `yale_connect=True` marker. Gated on payload[1]/[2] being 0/1 to avoid colliding with LockID-shape variant. State bits flagged for privacy (commissioning-window timing exposure) |
| 128 | xiaomi-hm-health | wearable (Mi Band / Amazfit / Zepp) | consumed | `plugins/huami_amazfit.py` (new) | service UUID `0x0000FEE0` → `mi_band_legacy`, `0000FED0-…` → `huami_new`. Name regex covers MI Band / Mi Smart Band / Amazfit / Zepp with model-tail → `model_hint`. Mi Scale path already in `mi_scale.py` (intentionally not duplicated) |
| 129 | yeelight-fluid | smart-lighting (Matter + JieLi audio) | skip-covered | — | Matter-over-BLE `0xFFF6` already covered by `matter.py`; JieLi RCSP `0xAE00` is transient OTA-only. Yeelight-direct UUIDs not statically extractable (libapp.so missing) |
| 130 | meross-meross | smart-home (multi-stack) | consumed | `plugins/meross.py` (new) | v1 service UUID `0x0000A00A` + name regex extracting 12-char hex BD_ADDR from `RFBL_`/`MRBL_` prefix (used as identity-hash basis); v2 MS605 SIG "test" CID `0xFFFF` + TLV decode (`subdev_type=0xC0`=`ms605`, gated on TLV shape to avoid claiming every 0xFFFF advertiser). Matter MSMC variants intentionally not duplicated (already in matter.py) |
| 131 | samsung-accessory-fridaymgr | audio (Galaxy Buds) | skip-no-ble | — | Galaxy Buds use BT Classic (RFCOMM SPP); this companion package contains zero BLE scan / GATT code. Fast Pair `0xFE2C` already covered by `plugins/fast_pair.py` |
| 132 | lgeha-nuts (LG ThinQ) | appliance (washer/fridge/AC/oven) | consumed | `plugins/lg_thinq.py` (new) | LG CID `0x00C4` (196) + name regex `^(AD_\|ad_\|LG_Smart\|LG_WashTower2\|LGE\|Signature)`. Mfr-data decode: 4B vendor prefix + 6B embedded MAC (identity-hash basis — survives address randomization) + UTF-8 model_name + LSB of last byte = `registered` flag. `provisioning_mode=True` flag for AD_-prefixed names or registered=False (chatty pre-onboard window flagged for privacy). Soundbar/XBoom/Smart-Lock variants deferred |
| 133 | ge-kitchen | appliance (GEA/SmartHQ) | skip-opaque | — | string-encrypted BleConstant blocks the GEA Beacon CID + name prefix; only `0xB0` / `0xB2` byte signals recoverable. Without the CID, false-positive risk is high. Re-visit after Stage 6d Frida decryption pass |
| 134 | dreame-smartlife | smart-home (Dreame/Mova) | enriched | `plugins/dreame.py` (existed) | added Xiaomi MiIO service UUID `0xFE98` + service-data ASCII prefix gating (`dreame`/`mova` case-insensitive). MiIO UUID without the brand prefix returns None (avoid claiming other Xiaomi-ecosystem devices). `mova` brand → `device_class=lawn_mower`. Legacy `0xFD92` + `^DL-` path preserved |
| 135 | sharkninja-shark | smart-home (vacuum) | consumed | `plugins/sharkninja.py` (new) | service UUID `0xFCBB` (SIG-allocated SharkNinja). Presence-only beacon with `provisioning_mode=True` flag — Shark stops advertising once Wi-Fi-paired, so a hit means onboarding. Byte-level decode in libsharkclean_android.so deferred |
| 136 | roborock-smart | smart-home (vacuum) | consumed | `plugins/roborock.py` (new) | two service UUIDs: `726F626F-…1071` (steady-state, paired) → `lifecycle=steady_state` and `…1051` (provisioning) → `lifecycle=provisioning`. Name regex `<vendor>-<model>_<deviceID>` → `model_token` + `device_identifier` (identity-hash basis). The `726F626F-` prefix is ASCII "robo" — cleartext vendor fingerprint |
| 137 | tado | thermostat (Matter only) | skip-covered | — | tado° is Matter-commissioning-only on BLE; `0xFFF6` already covered by `matter.py`. Post-commissioning, devices switch to Thread/Wi-Fi. Matter VID `0x1237` (tado) is informational only |
| 138 | coros-yf-smart | wearable (GPS watch) | skip-opaque | — | Hermes RN with encrypted asset bundles; 0 native libs in base APK; zero BLE references in Java tree. Custom RN bridge with no public protocol. Re-visit after XAPK refetch + Frida pass |
| 139 | garmin-connect | wearable (multi-product) | enriched | `plugins/garmin.py` (existed) | added GFDI control service UUID `6A4E4350-667B-11E3-949A-0800200C9A66` (post-paired) alongside existing `0xFE1F` modern + `00001001-7791-…` legacy UUIDs. Existing 23-family name catalog and CID `0x0087` decode preserved |
| 140 | philips-sleepmapper-root | medical (CPAP) | consumed | `plugins/philips_dreamstation.py` (new) | single 128-bit vendor UUID `22A4E311-A097-4517-9B81-CF32AF60B982` (DreamStation 2). Presence-only → `device_class=cpap`, `safety_critical=True`. PHI-by-inference equivalent to ResMed `0xFD56`. Legacy DS1 is BT-Classic SPP, out of scope |
| 141 | daimler-mercedesme | vehicle (no in-app BLE) | skip-no-ble | — | Mercedes me has zero in-app BLE scanning; CCC DK 3.0 is GMS-side. `getBondedDevices()` + `MBUX` substring match against Classic-BT names is the only discovery primitive (not BLE-visible) |
| 142 | lezyne-gpsally | fitness (cycling computer) | consumed | `plugins/lezyne.py` (new) | single 128-bit vendor UUID `904D0001-2CE9-078D-944D-263FD93D95B2`. Presence-only beacon → `device_class=cycling_computer` |
| 143 | xiaomi-smarthome | smart-home (Mi Home) | skip-covered | — | Xiaomi MiBeacon `0xFE95` (FrameControl + ProductID + FrameCounter + MAC + capability + objects/events) already covered by `mibeacon.py`. Matter `0xFFF6` covered by `matter.py`. Xiaomi explicitly does NOT use manufacturer-specific data |
| 144 | fitbark-com-android | tracker (pet collar) | consumed | `plugins/fitbark.py` (new) | three UUIDs: v1 `46697442-…6f66` ("FitBar!WooWoowoof"), v2/GPS/v4 `46697442-…6f67` ("...woog"), and v1 16-bit alias `0xFFA0`. UUID → `generation` (v1 vs v2_plus). Presence-only |
| 145 | suunto-android | wearable (Race/Vertical/7/9) | consumed | `plugins/suunto.py` (new) | NG UUID `61353090-…` (Race/Vertical/9 Peak Pro) → `protocol=ng`; NSP UUID `98AE7120-…` (Suunto 7/9) → `protocol=nsp`; `^Suunto ` name regex. SIG HR `0x180D` intentionally NOT matched (vendor-agnostic) |

| 146 | tacx-android | fitness (smart trainer) | consumed | `plugins/tacx.py` (new) | three vendor service-UUID families: `8EC90001-…` (Neo/Flux), `FE03A000-…` + `FE031000-…` (Smart Bike), `669A**05-…` legacy (5 model variants A2/A3/A4/A5/A6). Name regex `^TACX `. Generic SIG profile UUIDs (FTMS / CPS / FE1F) intentionally NOT matched here |
| 147 | skullcandy-skulliq | audio (earbuds/headphones) | consumed | `plugins/skullcandy.py` (new) | Skullcandy CID `0x07C9` (1993) + 8-entry model-ID table (Grind/Grind Fuel/Push Active/T80+/CHP200/T99+/T120/CHP300). Decodes `payload[2..7]` as 6-byte BD_ADDR → `embedded_mac` (identity-hash basis). Plus name regex covering Grind/CHPxxx/Txx/Method/Skullcandy CRUSHER/Sesh Boost/Push Active |
| 148 | makewonder-blockly | toy (Wonder Workshop robot) | consumed | `plugins/wonderworkshop.py` (new) | two service UUIDs: `AF237777-…` (Dash) and `AF237778-…` (Cue/Dot). Family auto-tagged. Per-device 4-char serial in local name (e.g. `Dash 0123`) |
| 149 | philips-cdp-ohc-tuscany | toothbrush (Sonicare) | skip-covered | — | already covered by `plugins/philips_sonicare.py` (service UUID `477EA600-A260-…` + Sonicare name regex). Possible CID `0x00DD` mfr-data is not parsed by the app — would need live capture |
| 150 | zengge-magiclight | smart-lighting (LED bulb) | consumed | `plugins/zengge.py` (new) | 7-prefix name regex (LEDBlue/LEDBLE/LEDSpeaker/LEDShoe/LEDnet/FluxBlue/TIBURN) is the gating signal — HM-10 `0xFFE0` UUID alone would false-positive on countless commodity products. `brand` extracted; `hm10_service_seen` flag when both coincide. Presence-only |
| 151 | myuvo-link | vehicle (Kia Access) | skip-no-ble | — | Kia Access has zero in-app BLE; CCC DK 3.0 delegated to GMS DCK / Samsung DKey. Same architectural pattern as Hyundai Bluelink / Mercedes me |
| 152 | latsen-pawfit | tracker (pet collar) | skip-needs-name-capture | — | Pawfit's app uses `setDeviceName(<name>)` with caller-supplied string (not statically extractable). Suspected `PT3-…`/`PNeoP3-…`/IMEI-as-name format unconfirmed. Re-visit after live-device sniff |
| 153 | wearables-maestro-companion | audio (Pixel Buds) | skip-covered | — | Pixel Buds use Fast Pair `0xFE2C` exclusively, already covered by `plugins/fast_pair.py`. Companion app does no BLE scanning (uses Bonded-Device-Walk + SDP). Post-pair traffic is BT-Classic SPP |
| 154 | airrobo-iot | smart-home (Tuya white-label) | skip-covered | — | Tuya `0xFD50` + CID `0x07D0` already in `tuya.py`; SIG Mesh `0x1827`/`0x1828` in `bt_mesh.py`. No AIRROBO-specific BLE signal |
| 155 | eco-global-app | smart-home (ECOVACS DEEBOT/WINBOT) | consumed | `plugins/alibaba_ais.py` (new) | Alibaba CID `0x01A8` (424) + 3-subtype dispatch (basic/beacon/gma keyed on payload[0] high nibble) → PID + 6-byte BD_ADDR (true MAC, identity-hash basis — defeats BLE address randomization). Name regex `^(DEEBOT\|WINBOT\|ECOVACS\|ALI-)`. Subtype gate ensures we don't claim iRobot/Mammotion sightings (they share CID 0x01A8 but use different payload shapes) |
| 156 | wyze-hualai | smart-home (Lock + EarBuds) | consumed | `plugins/wyze.py` (new) | Wyze EarBuds via SIG UUID `0xFD7B` → `product_class=earbuds`. Wyze Lock family via name regex `^(Wyze (Lock\|Lock Bolt\|Lock Keypad\|Gunsafe)\|DingDing)` → `product_class=lock` with variant tagging. Battery `0x180F` UUID alone NOT matched (vendor-agnostic); confidence=high when name + 0x180F coincide. `Wyze Gunsafe` carries `sensitive=True` (firearm-storage privacy flag) |

## Summary (as of 2026-05-01) — all 158 reports processed ✅

- Total reports: **158**
- consumed: **108** (batch 20 added: alibaba_ais, wyze)
- covered (pre-existing plugin): **4** (huahuacaocao→mibeacon, evehome→matter, switchbot→switchbot, dewalt-sitemanager→dewalt+apple_proximity)
- enriched: **17** (mopeka, weber-igrill, jbl-stc-com, hatchbaby-rest, sphero-spheromini, bose-monet, matter[opcode-fix via tplink-iot], sony_audio[via sony-songpal], acaia_scale[via breville-connectedcoffee], resmed[+AirMini/AS11/AirCurve/NightOwl/POC], renpho[+Qingniu CID 0x0157 + OEM catalog], sonos[+FE07 UUID + name], alivecor_ekg[+Kardia 6L/Card UUIDs], nespresso[+Aeroccino UUID + UUID-only path], august_yale[+yale-connect 3-byte decode], dreame[+MiIO FE98 + dreame/mova prefix], garmin[+GFDI UUID])
- pending: **0** ✅
- skip: **33** (eufylife-smarthome packed; mysugr/lifescan SIG-only; stationdm-bluelink/stationdm-genesis no-BLE; breville-connect Tuya-covered; breville-cookingapp opaque; resmed-airmini BR/EDR-only; googleadm tampered; medtronic-minimedmobile UI-shell; sengled-life3 mesh-covered; homeassistant-companion-android no-peripheral; edifier-connect needs-product-db; ledvance-smartplus mesh-covered; tracki-pet opaque; eufy-security packed; sensibo-app needs-libapp-so; yeelight-fluid mesh-covered; samsung-fridaymgr no-ble; ge-kitchen opaque; tado mesh-covered; coros-yf opaque; daimler-mercedesme no-ble; xiaomi-smarthome mibeacon-covered; philips-cdp-ohc-tuscany sonicare-covered; myuvo-link no-ble; latsen-pawfit needs-name-capture; wearables-maestro fast-pair-covered; airrobo-iot tuya-covered)

> **Marker convention**: each consumed/enriched/skip report gets an `> **adwatch status**: …` block at the top citing the plugin file (or skip reason) and what was usable. Updates leave a separate line below the original — never overwrite.
